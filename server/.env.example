JWT_SECRET=                          # strong random secret, e.g. 64+ hex chars from openssl rand -hex 48
ENCRYPTION_KEY=                     # exactly 32 characters, e.g. from openssl rand -hex 16
PORT=3000
ALLOWED_ORIGIN=http://localhost:3000
